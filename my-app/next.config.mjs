/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['2.bp.blogspot.com', 'www.caracteristicass.de','firebasestorage.googleapis.com'],
    },
  };
  

export default nextConfig;
